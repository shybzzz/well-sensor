import { SensorTypeIconPipe } from './sensor-type-icon.pipe';

describe('SensorTypeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SensorTypeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
