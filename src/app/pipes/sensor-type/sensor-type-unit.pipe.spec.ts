import { SensorTypeUnitPipe } from './sensor-type-unit.pipe';

describe('SensorTypeUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new SensorTypeUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
