import { SensorTypePipe } from './sensor-type.pipe';

describe('SensorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SensorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
